machine:
  java:
    version: openjdk8
dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter build-tools-23.0.3
test:
  override:
    - cd BeerJournal && ./gradlew testDebug
    - cp -r BeerJournal/build/reports/tests/debug/* $CIRCLE_TEST_REPORTS
